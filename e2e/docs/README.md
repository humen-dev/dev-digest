# e2e — docs

**Detailed** design docs for `@devdigest/e2e` that are too heavy for
[`../CLAUDE.md`](../CLAUDE.md) (which stays a short map). Add one Markdown file
per topic and link it below; Claude reads these on demand, not every session.

Good fits: runner internals (`run.ts`), the hermetic-stack setup, agent-browser
locator strategy, adding a new flow, CI wiring, ADRs.

## Index
- [`hermetic-stack.md`](./hermetic-stack.md) — the runner, hermetic ports, determinism rules
- _(add docs here, e.g. `writing-a-flow.md`)_
