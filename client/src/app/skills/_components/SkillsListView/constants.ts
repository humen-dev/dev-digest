/** Constants for the Skills list view. */

/** Card grid template (responsive auto-fill) — same as the Agents list. */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(280px, 1fr))";

/** Width of the side panel that previews a skill's body (px). */
export const PREVIEW_PANEL_WIDTH = 620;

/** Width of the create-skill modal (px). */
export const CREATE_MODAL_WIDTH = 720;

/** Type preselected in the create-skill modal. */
export const DEFAULT_SKILL_TYPE = "rubric" as const;
