/** Constants for the Add Skill modal. */

/** Width of the modal (px). */
export const ADD_MODAL_WIDTH = 640;

/** Type preselected in the Create tab. */
export const DEFAULT_SKILL_TYPE = "rubric" as const;

export const TAB_CREATE = "create";
export const TAB_FILE = "file";
export const TAB_URL = "url";
