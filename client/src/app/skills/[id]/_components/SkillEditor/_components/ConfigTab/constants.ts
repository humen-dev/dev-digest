import type { SkillType } from "@devdigest/shared";

/** Selectable skill types in the Config form. */
export const TYPE_OPTIONS: readonly SkillType[] = ["rubric", "convention", "security", "custom"];
